# Personal AI Runtime V0.1 Readiness — S6

- 기준: `devlop`, S6 시작 HEAD `fa83746a170c26074f460eb42afa16b42197fe9c` + 이번 S6 변경.
- 검증일: 2026-09-16 (KST).
- 환경: macOS/Darwin arm64, Node `v26.7.0`, 로컬 파일 시스템·Git·POSIX process group.
- 결론: **제한된 현재 범위의 V0.1 RC 검증 진입은 조건부 가능하다. release 선언은 아니다.** 실제 GPT/DeepSeek, 다른 OS/최소 Node 버전 및 전체 저장소 회귀는 별도 검증이 필요하다.
- 기준 문서: [MASTER_SPEC](MASTER_SPEC_PI_PERSONAL_AI_RUNTIME.md), [ARCHITECTURE](ARCHITECTURE.md), [DECISIONS](DECISIONS.md), [IMPLEMENTATION_PLAN](IMPLEMENTATION_PLAN.md), [WORK_LOG](WORK_LOG.md) LOG-012.

## 1. 판정 규칙과 MASTER_SPEC DoD

- **PASS:** 명시된 지원 범위에서 현재 작업 중 검증했다. 모든 환경·입력에서의 증명이 아니다.
- **PARTIAL:** 구현/검증한 부분과 제한이 함께 있다.
- **UNSUPPORTED:** 의도적으로 실행하지 않는다. 차단 테스트 통과와 기능 지원을 혼동하지 않는다.
- **NOT VERIFIED:** 이번 단계에서 실제 환경 검증을 수행하지 않았다.

| MASTER_SPEC §34 항목 | 판정 | 확인된 범위 / 남은 점 |
|---|---|---|
| Pi에서 실제 실행 가능 | PASS | 명시적 Extension 로딩과 실제 TUI/faux run·cancel·reload·승인 중단 |
| QUICK / STANDARD / COMPLEX 판정 가능 | PASS | 세 분류와 최소 팀 선택의 규칙 테스트. 자연어 전반의 분류 정확도는 별도 문제 |
| COMPLEX 실행 | UNSUPPORTED | 판정 후 명시적으로 거부. Lead/Planner 실행 없음 |
| Risk R0~R3 판정 가능 | PARTIAL | goal/action/path 규칙과 알려진 dependency·보호 파일·한정 삭제 판정. 의미적 위험·숨겨진 credential·프로그램 내부 동작은 완전 분석하지 않음 |
| STANDARD Developer → Reviewer | PASS | 독립 SDK 세션·권한·구조화 handoff/실제 diff/check 자료·완료 guard |
| Reviewer REVISE 재작업 | PASS | STANDARD 0~3회(기본1), 새 attempt/session, 이전 PASS/참조 재사용 차단. QUICK/R3는0 |
| 위험 Tool 차단 또는 승인 | PARTIAL | worker gate와 bound R2, 한정 R3 한 파일 삭제 승인. 범용 shell/deploy/install/history/대량 삭제는 미지원 |
| `.ai` 상태 저장 | PARTIAL | state 원본·tasks projection·config·명시적 decisions/check export. decisions는 운영 projection이며 기술 ADR 자동 작성 아님; 다중 파일 transaction 아님 |
| `/team` | PASS | 역할/profile/session 참조와 live/stored 출처 구분 |
| `/state` | PASS | read-only snapshot·checks/review/decision·export, 조회만으로 복구/승인 변경 없음 |
| `/workflow` | PASS | run/status/history/config/cancel·중복 writer 방지·lifecycle 연결 |
| `/risk` | PASS | 초기 risk와 action/approval 기록을 구분해 표시. risk 의미 분석의 제한은 위 항목과 동일 |
| 가능한 build/test/lint evidence | PARTIAL | 등록 executable/argv/cwd/env의 실제 exit/output/timing/digest와 필수 check guard 검증. fixture는 Node check 중심; root check는 별도 개발 검증이고 임의 사용자 프로젝트 build/lint 성공을 뜻하지 않음 |
| 기존 Pi 핵심 기능 회귀 없음 | PARTIAL | 관련 기존 prompt/runtime/model-extension/retry-events/queue 및 public loader 회귀 통과. 전체 저장소 suite/build/e2e는 미실행 |
| 실제 GPT/DeepSeek smoke | NOT VERIFIED | faux 응답을 실제 Provider 품질·인증·네트워크 검증으로 대체하지 않음 |

## 2. 구현·검증된 기능

- QUICK/R0 read-only 및 QUICK/R1 고정 한 파일·100행 범위, Executor coding profile, 필수 두 검증 단계와 executorDigest.
- STANDARD/R1·사전 선택한 R2의 독립 Developer/Reviewer, actual Git baseline/digest, PASS/REVISE/BLOCK 및 필수 check guards.
- R3의 사전 선택한 Git-tracked 일반 UTF-8 파일 한 개(256 KiB 이하) 삭제: 기본 Deny, exact/expiring/one-use consent, 소비 증거 및 독립 review/검증.
- 단일 writer, action intent/result, atomic file rename, partial persistence 오류, 관찰용 RuntimeEvent, 명시적 취소와 부분 변경 보고.
- 읽기 전용 observations와 명시적 generated export. 수동/수정 파일을 강제로 덮어쓰거나 export를 실행 권한으로 읽지 않는다.

## 3. S6에서 강화한 실패 경계

| 문제 / 짧은 trace | 수정 및 검증 |
|---|---|
| tool effect → state save 실패 → Store가 즉시 lock 해제 → SDK cleanup은 아직 대기 | Store는 mutation만 닫고 lease는 owner의 close까지 유지. R1 write와 R3 delete 뒤 실제 rename 실패 + 지연 SDK cleanup에서 두 번째 writer가 거부됨 |
| 동시 close가 같은 lease를 두 번 해제하려는 race | close promise를 공유하고 완료 뒤 새 writer의 lock에는 손대지 않음을 검증 |
| SDK abort/dispose 예외인데 execute promise 정산만 보고 owner가 close | Pi adapter의 cleanup 확인을 별도 전달. 불확실하면 다음 역할/COMPLETE를 막고 lock 유지 |
| check runner 예외/cancel 중 예외에서 cleanup 값이 이전 true로 남음 | invocation 전 unknown으로 전환하고 확실한 반환만 확인으로 수락. 예외를 미실행/SKIPPED로 오인하지 않음 |
| Git evidence process 정리가 불확실한데 preflight 객체가 반환되지 않음 | Git adapter의 cleanup 상태와 명시적 cleanup 오류를 owner까지 전달. 추가 Git 수집 없이 unknown/lock retained로 보고 |
| 완료 후 보고용 Git 재수집이 새 cleanup 실패를 만듦 | COMPLETE 직전 검증한 workspace snapshot을 최종 보고에 사용; terminal commit 뒤 새 Git process를 시작하지 않음 |
| check exit 0과 cancel 경합 → PASS가 남거나 audit finish를 재시도 | settlement 전 취소는 PASS로 기록하지 않음. finish 실패를 다시 finish하여 partial commit 오류를 덮지 않음 |
| 새 session ID + 이전 attempt sessionFile 재사용 | ID와 파일 참조 모두 중복 거부 |
| FIFO state/config/lock 파일이 open 단계에서 무한 대기 가능 | POSIX nonblocking/no-follow descriptor 열기와 regular-file 검사. config directory/file identity·크기 검사도 강화 |

기능·권한·workflow 종류를 추가하지 않았다. 기존 safe path의 기본 정책과 결과 계약은 유지했다.

## 4. 이번 테스트 결과와 증거 위치

| 실행 묶음 | 결과 |
|---|---|
| company-runtime 기존 S0~S5D + S6 failure/kernel tests | 17개 파일 / 453개 PASS |
| coding-agent company-runtime suite + 기존 prompt suite | 8개 파일 / 287개 PASS |
| 추가 기존 Pi runtime/model-extension/retry-events/queue | 4개 파일 / 59개 PASS |
| 합계 | **29개 파일 / 799개 PASS** — 전체 저장소 suite 수 아님 |
| root `npm run check` | PASS — Biome/TypeScript/deps/entry graph/shrinkwrap/install-lock/browser smoke |
| `git diff --check` | PASS |

S6 신규 테스트는 unit/process/crash 23개, kernel freshness/race 11개, SDK/lifecycle 51개로 총85개다. 기존655개 회귀를 유지하고 기존 Pi59개를 추가로 실행했다.

- [hardening.test.ts](../packages/company-runtime/test/hardening.test.ts): actual POSIX process·TERM→KILL·출력 제한·EPERM cleanup 불확실성·FIFO·chmod/executable·실제 별도 Node process crash·stale lock·expiry 경계.
- [kernel-hardening.test.ts](../packages/company-runtime/test/kernel-hardening.test.ts): 이전 attempt의 review/check/sessionFile, stale live digest, result/abort 및 terminal commit 경계, observer 실패.
- [company-runtime-hardening.test.ts](../packages/coding-agent/test/suite/company-runtime-hardening.test.ts): Developer/Reviewer/Executor 연결·구조화/자연어/stream 실패, Reviewer auth/model 실패, late mutation, 실제 state 실패 중 lease 유지, cleanup 실패/예외·check/cancel 경합·R3 effect 실패.
- lifecycle matrix: Developer, Reviewer, check process, approval 대기 각각에 cancel/switch/fork/tree/reload/quit를 적용한 **24개 사례**. 정상 정리 순서는 `cancel → resource 종료 확인 → terminal state → unlock`으로 검증했다.
- S0~S5D의 기존 state/Policy/approval/export/observations·SDK 회귀도 이번에 실행했다. 과거 테스트 결과를 현재 PASS에 합산하지 않았다.

### 실제 interactive faux smoke

80×24 tmux + 저장소 `pi-test.sh` + 명시적 `-e` wrapper, 별도 HOME/agentDir, offline/빈 자격 환경, temporary Git fixture를 사용했다.

1. STANDARD 성공: COMPLETED, SELF_CHECK/TEST PASS, lock 없음.
2. active Developer에서 `/workflow cancel`: CANCELLED, 기존 부분 변경 유지, 늦은 write 차단, lock 없음.
3. active Reviewer에서 `/reload`: 종료 후 CANCELLED 저장, reload 뒤 status 조회, 이전/늦은 PASS 미사용.
4. active verification에서 `/reload`: CANCELLED/FAIL, 원래 process group 소멸 확인, 지연 write 없음, lock 없음.
5. active R3 approval modal에서 Esc: DENIED/BLOCKED, 파일 유지, reload 뒤 risk/approval 조회.

실제 TUI switch/fork/tree 전환 자체는 이번 smoke에서 조작하지 않았다. 해당 Company lifecycle 경로는 24개 Host-handler matrix와 기존 Pi runtime 전환 tests로 검증했다. faux는 실제 SDK/파일/check/lifecycle 통합 증거이지 실제 LLM 품질 증거가 아니다.

## 5. 실제 Provider 미검증과 RC 단계

**NOT VERIFIED:** GPT/DeepSeek 실인증/API 모델 가용성, 실제 HTTP disconnect·rate limit·quota·proxy/TLS·OAuth 갱신, 실제 모델의 tool/schema 준수·문제 해결 품질·독립 리뷰 품질·비용/지연.

RC 단계에서는 사용자가 승인한 별도 disposable Git 프로젝트·정확한 profile/model·검증 명령으로 실행한다. 자격 정보는 Pi 인증 범위에만 두고 source/state/work log에 복사하지 않는다. 성공 trace뿐 아니라 실제 cancel/stream 중단/인증 실패도 검증하고 faux와 별도 결과로 기록한다. S6는 유료 호출을 수행하지 않았다.

## 6. 플랫폼 경계

| 영역 | 현재 확인 / 제한 |
|---|---|
| macOS arm64 / Node26.7 | 현재 actual FS/Git/process/TUI 테스트 환경 |
| Linux/다른 POSIX | 설계상 POSIX API 사용. 이번 작업의 실제 실행은 NOT VERIFIED |
| 최소 Node22.19·Bun | 이번 실행 환경이 아님. 별도 RC 호환성 검증 필요 |
| Windows execution | UNSUPPORTED. Runtime owner는 writer 획득 전에 거부. 음수 PID group signal, detached 세션, no-follow/nonblocking FD, unlink/rename 의미를 Windows 호환이라고 주장하지 않음 |
| paths | canonical root·literal 상대 경로·symlink/hardlink 거부. drive/UNC/backslash/traversal 테스트는 Windows 지원 테스트가 아님 |
| executable | shell:false + argv, absolute/PATH resolution, executable access·공백 경로 테스트. PATH/프로그램은 trusted Host 입력 |
| process group | 원래 POSIX group에 TERM/KILL 후 close/소멸 확인. 별도 group으로 탈출한 daemon을 추적/격리하지 못함 |
| lock/rename | 로컬 O_EXCL owner token/inode, 파일별 temp/sync/rename. NFS/SMB·전원 장애·directory fsync·다중 파일 transaction은 NOT VERIFIED 또는 미제공 |
| permissions | private state/temp 0600, directory0700, regular-file checks. root 사용자/ACL/다른 FS 의미까지 증명하지 않음. 이번 EACCES effect-failure 검증은 비특권 macOS 사용자에서 실행 |

## 7. 보안·취소·commit 경계

- trusted local Host/Extension/Provider adapter/등록 check를 위한 협력적 gate다. 임의 Node 코드나 등록 프로그램의 내부 I/O를 가로채는 OS sandbox가 아니다.
- cleanup proof는 관리하는 SDK 작업과 원래 process group에 한정된다. `safeToRelease=false`면 FAILED/CANCELLED라는 Run label만 보고 lock을 지우면 안 된다. **worker/process 종료와 소유권을 수동 확인하기 전 추가 workspace 작업을 하지 않는다.** 자동 PID 기반 lock 탈취는 없다.
- custom AgentExecutor/Verifier는 promise 정산 전에 자원을 정리하거나 cleanup 미확인을 명시해야 한다. Pi/파일 adapter는 해당 상태를 구현한다. Runtime이 임의 외부 Port 구현의 정직성을 증명하지는 않는다.
- 정상 취소는 종료를 기다려 terminal state를 기록한 후 unlock한다. 정리가 불확실하면 실패 진단/기존 PREPARED intent만 남을 수 있고 lock을 유지한다. 이를 성공 또는 실행 안 됨으로 추정하지 않는다.
- cancel은 COMPLETE commit 시작 전까지 수락한다. 모든 guard를 통과해 terminal save가 시작된 뒤의 cancel은 늦은 요청이며 그 commit을 rollback하지 않는다. 저장 실패라면 RunCompleted를 발행하지 않는다. per-file partial commit으로 durable state와 caller 결과가 다를 가능성은 그대로다.
- R3 expiry는 effect 직전까지 검사한다. 실행 뒤 만료됐다고 승인된 effect를 되돌리거나 다시 실행하지 않는다. 재시작은 미완료 grant/action을 INTERRUPTED로 남긴다.
- RuntimeEvent는 best-effort 관찰이다. emit 오류는 별도 진단이며 완료/실패를 재판정하지 않는다. outbox/replay/exactly-once 전달은 없다.

## 8. 알려진 위험과 미지원 기능

- 비협조 Provider/auth/event sink/파일 I/O는 cancellation/shutdown을 오래 지연시킬 수 있다. timeout은 강제 OS 종료의 절대 기한이 아니며 이 경우 lease를 유지한다.
- 뜻밖의 SDK 생성/cleanup 또는 process runner 예외는 실제 자원이 없었어도 보수적으로 lease를 남길 수 있다. 자동 해제보다 수동 확인을 우선한다.
- status/read-only snapshot은 live filesystem 검증이나 프로세스 생존 증명이 아니다. 오래된 PASS·partial state/tasks·메모리 전용 진단 손실을 함께 해석해야 한다.
- 실제 effect와 durable result 사이에는 transaction이 없다. 승인된 삭제나 write 뒤 저장 실패가 남을 수 있다. 자동 rollback/checkpoint/resume 없음.
- risk/요구사항 판정은 heuristic·모델 판단·등록 check 품질에 의존한다. semantic safety의 수학적 증명, 모든 secret/manifest 탐지, credential-free stdout 보장은 없다.
- 파일/출력/state 크기 제한과 보수적 경로 거부, dirty baseline 거부, export 수동 소유권 충돌은 유지된다. generated checksum은 인증 서명이 아니다.
- UNSUPPORTED: Lead/Planner·COMPLEX 실행·parallel agents·generic R3·arbitrary shell·deploy/install 권한 확대·DAG scheduler/renderer·T3Code·RPC/Web UI·auto resume/checkpoint·model fallback/cost routing·SQLite·신규 OS sandbox·패키지/브랜딩 migration.
- 전체 테스트/build, live Provider 및 dependency vulnerability 재평가는 이번에 실행하지 않았다. 과거 WORK_LOG의 dependency audit를 현재 보안 통과로 주장하지 않는다.

## 9. RC 진입 판정

**조건부 가능:** 제한된 지원 범위에서 로컬 faux·실제 파일/process·선택한 Pi 회귀와 interactive 실패 경계를 검증했다. 위 PARTIAL/UNSUPPORTED/NOT VERIFIED를 사용자에게 그대로 공개해야 한다.

다음 RC gate는 별도 승인 후 수행한다: 실제 GPT/DeepSeek disposable-project smoke, 선택 모델/auth/env 확정, 지원할 OS/Node 버전 검증, 프로젝트별 build/lint/test 검증, 패키지 취약점·배포/설치 검증 범위 결정. S6 결과만으로 V0.1 release/일반 production 안전성을 선언하지 않는다.
